<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Reclaim</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<!-- Document 07 §5 / Document 03 §8. Only the two permissions the app
	     actually uses. NSPhotoLibraryAddUsageDescription intentionally
	     omitted per Document 14 §3.4 point 2 — the app only ever deletes
	     assets via PHPhotoLibrary.performChanges, never adds/saves any,
	     so the "add" usage key should not be required. FLAGGED FOR
	     PHASE-4-STYLE CONFIRMATION ON FIRST REAL BUILD: if Xcode/the OS
	     unexpectedly prompts for this key at runtime, add it back with an
	     accurate description rather than assuming this omission is
	     correct without that confirmation (Document 13 §3.1 point 3). -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Reclaim scans your photos on your device to find duplicates, similar photos, screenshots, and large videos you can delete. Nothing is uploaded — analysis happens entirely on your iPhone.</string>

	<key>NSContactsUsageDescription</key>
	<string>Reclaim scans your contacts on your device to find likely duplicates. Nothing is uploaded — analysis happens entirely on your iPhone, and you review every match before anything is deleted.</string>

	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
</dict>
</plist>
