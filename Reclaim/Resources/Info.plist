<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Reclaim</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<!-- Only the two permissions the app actually uses (Document 07 §5 /
	     Document 03 §8). NSPhotoLibraryAddUsageDescription is intentionally
	     omitted: the app only ever deletes assets via
	     PHPhotoLibrary.performChanges, never adds or saves any. FLAGGED FOR
	     CONFIRMATION ON FIRST REAL BUILD: if the OS unexpectedly refuses a
	     deletion at runtime for a missing add-usage key, add it back with an
	     accurate description rather than assuming this omission is correct
	     (Document 13 §3.1 point 3). -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Reclaim scans your photos on your device to find duplicates, similar photos, screenshots, and large videos you can delete. Nothing is uploaded — analysis happens entirely on your iPhone.</string>

	<key>NSContactsUsageDescription</key>
	<string>Reclaim scans your contacts on your device to find likely duplicates. Nothing is uploaded — analysis happens entirely on your iPhone, and you review every match before anything is deleted.</string>

	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
</dict>
</plist>
